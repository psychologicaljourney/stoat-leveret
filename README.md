# stoat-leveret

## Running the bot
1. must have a `.env` file inside the main directory and `tags.db`, `perms.db` and `reminder.db` inside `src/db`
2. run with npm start

## Frequently Asked Questions
### Why does the bot not work when started without throwing any errors?
I fully blame the stoat servers so just keep restarting the bot until it works :D